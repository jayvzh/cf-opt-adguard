# cf-opt-adguard 开发规则（精简版 v0.1）

> 给 AI 的第一份规则。日常任务先执行本页即可，能省 token 就不必打开完整总则。
> 完整总则：[docs/DEVELOPMENT_RULES.md](docs/DEVELOPMENT_RULES.md)（架构判断、完整红线、交付自检时读；冲突以它为准）。
> 宗旨：先查文档、少扫描、小步改、多复用、不超范围、无须每一小步都验证。

## 0. 项目一句话

Go 单二进制 CLI（本机 go1.27.1，module path `cf-opt-adguard`）：从 AdGuard Home 日志发现高频 Cloudflare 域名，探测确认后把优选 IP 增量写成 AGH DNS 重写。当前 **M1–M4（dry-run 全链路）已实现**，`syncer` / `verifier` 属 M5–M6 待建。

## 1. 文档地图（按任务点开，禁止整目录通读）

| 任务 | 打开 |
| --- | --- |
| 判定需求范围 / 非目标 / 默认护栏 | [docs/PRD.md](docs/PRD.md) |
| 流水线 / 探测打分 / 状态机 / 失败策略 | [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) |
| 包放哪 / 依赖方向 / 阶段 I/O | [docs/PROJECT_STRUCTURE.md](docs/PROJECT_STRUCTURE.md) |
| 表结构 / 配置键 / 枚举 | [docs/DATA_MODEL.md](docs/DATA_MODEL.md) |
| AGH / CF / CFST 接口字段 / CLI flags | [docs/API.md](docs/API.md) |
| 背景现状 / 关键决策结论 / 下一步 | [docs/context.md](docs/context.md) |
| 选型理由 / 新依赖 | [docs/decisions.md](docs/decisions.md) |
| AGH 版本差异 / 已知坑 | [docs/pitfalls.md](docs/pitfalls.md) |
| 缩写 / 目录约定 / 技术债绕法 | [docs/conventions.md](docs/conventions.md) |

## 2. 铁律（永久生效，违反即返工）

1. 只做 P0 链路；P1（通知 / 内置调度 / 回滚 / 多实例）、P2（Web UI）禁止提前实现。
2. 只有 syncer 能调 AGH 写接口，且必须消费 planner 的计划；禁止全量删除再添加，禁止碰托管集合之外的用户手工规则。
3. **验证红线**：测试一律 `httptest` 假服务器 + 夹具，禁止对真实 AGH / 公网做写操作验证；对真实实例只允许 dry-run 只读，live 写入需用户明确授权。
4. 外部接口字段以目标实例实测为准，禁止照抄 PRD / 搜索结果；发现差异先改 [API.md](docs/API.md) + 记 [pitfalls.md](docs/pitfalls.md)，再写代码。
5. rewrite answer 只写裸 IP，禁止写优选域名 / `$dnsrewrite=`；默认 zone 混合归并防误伤（D14）：通配仅在 zone 内全部候选 confirmed 且 `sync.wildcard: true` 时出现，混入不确定域名自动回退精确，`exact` 为显式退回项。
6. 探测只用用户配置的独立 resolver，禁止偷用系统默认（会被自己的重写污染）。
7. 默认 dry 模式（只出计划），显式 `--apply` 才写入；优选 IP 为空或采集 / list 等前置阶段失败时，禁止任何写 / 删操作。
8. 业务默认值只准在 config 定义一次；`references/` 只读，禁止 import 或大段拷贝。

## 3. 省 token 的行为约束

- 先查文档地图再动手；检索限定到相关包；大文件分段读 / 关键词定位，不通读参考仓库。
- 小步改，一个阶段一个阶段落地；聚合、打分、diff 先写纯函数 + 表驱动测试。
- 不臆造字段与命令，结论先行；验证阶梯的展开见完整总则 §8。

## 4. 常用命令（工程初始化后回填实测）

- 当前环境：`go version`（本机 go1.27.1，module path `cf-opt-adguard`）。
- 初始化后标准命令：`gofmt -l .`、`go vet ./...`、`go build ./...`、`go test ./...`。
- 冒烟（默认即 dry-run，对真实实例只读）：`go run ./cmd/cf-opt-adguard run -c config.yaml`；确认计划后才允许 `--apply`。
- 命令以脚本 / `--help` 实际输出为准；改了 flags 同步改 API.md §7 与 README。

## 5. 交付自检（10 秒）

- 写调用是否都出自 syncer + planner 计划？有没有为调试对真实 AGH 发写请求？
- 测试是否只用 httptest / 夹具，不依赖网络与真实实例？
- answer 是否全是裸 IP？通配是否只在 zone 全 confirmed 且 wildcard 开启时出现（D14）？
- 接口字段是否与实测一致、差异入了 pitfalls？默认值是否只在 config 一处？
- 有没有提前实现 P1 / P2？受影响文档是否同批更新？
- 完整清单：[docs/DEVELOPMENT_RULES.md](docs/DEVELOPMENT_RULES.md) §10。
